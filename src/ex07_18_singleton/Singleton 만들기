*싱클톤(singleton)
하나의 애플리케이션 내에서 단 ㅏ하나만 생성되는 객체

*싱글톤을 만드는 방법
(1)외부에서 new 연산자로 생성자를 호출할 수 없도록 막기
	private 접근 제한자를 생성자 앞에 붙임

(2) 클래스 자신의 탕ㅂ으로 정적 필드 선언
	자신의 객체를 생성해 초기화
	private 접근 제한자 붙여 외부에서 필드 값 변경 불가하도록 한다
	
(3) 외부에서 호출할 수 잇는 정적 메소드인 getInstance() 선언
	정적 필드에서 참조하고 있느 자신의 객체 리턴		